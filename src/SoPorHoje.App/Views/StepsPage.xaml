<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SoPorHoje.App.ViewModels"
             xmlns:controls="clr-namespace:SoPorHoje.App.Controls"
             x:Class="SoPorHoje.App.Views.StepsPage"
             x:DataType="vm:StepsViewModel"
             Title="A Trilha"
             BackgroundColor="{StaticResource BgPrimary}"
             SemanticProperties.Description="A Trilha — princípios de recuperação">

    <ScrollView Style="{StaticResource PageScrollView}">
        <VerticalStackLayout Padding="16,12,16,32" Spacing="0">

            <!-- Header -->
            <Label Text="A Trilha"
                   Style="{StaticResource PageTitle}"
                   Margin="0,4,0,4"
                   SemanticProperties.HeadingLevel="Level1"
                   SemanticProperties.Description="A Trilha" />
            <Label Text="Princípios para caminhar, um dia de cada vez"
                   Style="{StaticResource MutedText}"
                   Margin="0,0,0,20"
                   SemanticProperties.Description="Princípios para caminhar, um dia de cada vez" />

            <!-- Accordion de Passos -->
            <CollectionView ItemsSource="{Binding Steps}"
                            SelectionMode="None"
                            SemanticProperties.Description="Lista de princípios da Trilha">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:StepItem">
                        <controls:AccordionItem
                            Number="{Binding Number}"
                            Title="{Binding Title}"
                            Body="{Binding Text}"
                            IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                            Margin="0,0,0,0" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
